.git
.gitignore
.env
.env.example
credentials.json
token.json
data/google_token.json

# Local directories
__pycache__/
*.pyc
frontend/
docs/

# Do not ignore data folder itself if we need to mount to it, but ignore its local contents (except maybe keep the folder)
data/*
!data/.keep
